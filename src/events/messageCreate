const Discord = require('discord.js')
const { guildId, token, clientId, pingRoleId, pingChannelId, logChannelId } = require('../../config.json')
const sql = require('../utilities/sqlHandler')

module.exports ={
    name: "messageCreate",
    once: false,
    async execute(message) {
        if (message.content.test(/azuma/g) {
            message.channel.send("Azuma. Who is he exactly? Someone that simply gets angry over trivial things?")
        }
    }
}
